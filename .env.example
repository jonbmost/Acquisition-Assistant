# Server-side API key for Anthropic Claude
# Used by serverless API routes (api/chat.js, api/chat-with-mcp.js)
ANTHROPIC_API_KEY=sk-ant-your-key-here

# Optional: environment-specific overrides if you keep separate keys for preview/prod
# (Vercel sets VERCEL_ENV=preview for branch deploys and VERCEL_ENV=production for main)
# ANTHROPIC_API_KEY_PREVIEW=sk-ant-your-preview-key
# ANTHROPIC_API_KEY_PROD=sk-ant-your-production-key
